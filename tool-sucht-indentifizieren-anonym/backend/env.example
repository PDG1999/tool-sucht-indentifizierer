# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=samebi_db
DB_USER=samebi_user
DB_PASSWORD=your_secure_password

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=3001
NODE_ENV=development

# CORS Configuration (comma-separated for multiple origins)
# Lokale Entwicklung + Check-Tool + Dashboard für Berater/Supervisor
CORS_ORIGIN=http://localhost:3002,http://localhost:3004,https://check.samebi.net,https://dashboard.samebi.net

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
